/** T-06 — relative forward continuation over legitimately disclosed targets, bounded by `LH`. */
export type { ContinuationTimerHandle, ForwardContinuation, ForwardContinuationOptions } from './forward';
export { FORWARD_CONTINUATION_INTERVAL_MS, createForwardContinuation } from './forward';
