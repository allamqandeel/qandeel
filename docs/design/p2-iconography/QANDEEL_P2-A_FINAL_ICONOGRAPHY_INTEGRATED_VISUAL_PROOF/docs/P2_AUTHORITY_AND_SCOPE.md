# P2-A — Authority and Scope

**Status:** `P2-A — VISUAL PROOF / DECISION GATE — NOT A FREEZE`. **P2 is not closed.** This package makes no canonical
amendment and changes no lifecycle record.

**P2-A refinement (2026-09-26).** This is a small, Product-Owner-authorised refinement on the same branch and the same
Draft PR #273.
- It was made after independent review and the Product Owner's visual selections.
- Repository truth at the refinement:
  - `main` was still `209ca7269181e447f0c5192e49979aa391103871`;
  - PR #273 was Draft / open / unmerged at head `23c8af1029273c9a3a462bd3db90293419c3fa27`;
  - the laptop was on the branch at that head, clean, 0 ahead / 0 behind.
- Scope:
  - record the accepted selections (`P2_PRODUCT_PROOF_REPORT.md` §J);
  - polish the End Call glyph presence (§K; `P2_CALL_RAIL_PROOF.md` §6);
  - correct the Skia / Reanimated wording (`P2_REFERENCE_GATE.md` §2).
- It opens no new alternative, and it closes and freezes nothing. Canonical P2 closure is the later P2-B task.

## 1. Repository truth (task §1)

| Item | Value |
|---|---|
| Canonical GitHub `main` at task start | `209ca7269181e447f0c5192e49979aa391103871`, "docs: close P1 identity preferences and QANDEEL Understanding (#272)". It **equals** the SHA in the brief |
| Laptop repository | `E:\QANDEEL\QANDEEL PROJECT`, on `docs/p1-final-product-decision-closure` at `360be71`: clean, 1 ahead / 1 behind `origin/main`. The squash-merged P1 branch was neither diverged work nor a P2 attempt |
| Open pull requests | none |
| Existing P2 branches (local / remote) | none |
| Task branch | `design/p2-final-iconography-integrated-visual-proof`, created from `origin/main` `209ca72`. Nothing was reset or cleaned |

## 2. Authorities read before designing

**Entry points:**
- `AGENTS.md`
- `QANDEEL_CURRENT_STATE.md`
- `QANDEEL_PRODUCT_ROADMAP.md`
- `QANDEEL_PROJECT_MAP.md`
- `docs/qandeel-canonical-backlog-v1.md`
- `docs/design/canonical-artifacts/QANDEEL_CANONICAL_ARTIFACT_INDEX.md`, including its later-amendments table

**Conversation / Voice / Live Call:**
- G1.1 closure (§1 accepted shell; §3 `UTTERANCE`)
- G1.2 closure (§1–§6: Analysis-first, background, no normal-state prose, permission at first need, and **§6: the icons,
  audio strip and final iconography are NOT frozen**)
- G3 closure (§C.1, §C.2, §D, §F, §L)

**Analysis / shell / temporal composition:**
- G2 closure
- G2.3 closure
- the T-11 Return presentation amendment
- the G2 / F2 Analysis-shell amendment (the dark scope includes the rail, the call line and the Timeline)
- the T-11 / T-12 temporal orientation amendment (world → line → Timeline → OrientationChrome; Return Live at the Live
  edge; yield rule; 44-pt targets)

**Timeline, temporal navigation, motion, Return** (every binding clause was extracted with file and line; summarised in
`P2_TEMPORAL_SPINE_APERTURE_PROOF.md` §1):
- `timeline-presentation-window-v1` (T-05)
- `temporal-navigation-layer-v1` (T-06)
- `return-navigation-layer-v1` (T-07)
- `inspection-orientation-return-chrome-v1` (T-08)
- `living-analysis-map-motion-system-v1` (T-10)
- `responsive-recomposition-v1` (T-11)
- `visual-motion-authority-boundary-v1`
- `final-living-analysis-map-integration-v1` (T-12: its temporal, locale and slot sections)

**Material / state / accessibility / appearance / typography / brand:**
- C3 (`C3_ICONOGRAPHY_MATERIAL_CONTRACT.md` and the C3 tokens)
- E1R (the interaction tokens and the state map)
- B4R (roles and tones)
- F1R2 / F2 (targets, non-text contrast, Reduced Motion, Increase Contrast, light tokens)
- E3 typography (`system.json`)
- I-08B2.5 brand

**P1 and the product shell:**
- P1 closure: §8 General Settings (icons open), §10 Understanding (entry open), §12 appearance preference
- I-08A4: the Global Switcher's three areas
- I-08N-01: the unified Activity surface, with no entry placement

## 3. Frozen boundaries, consumed

The table records how the proof keeps each frozen boundary.

| Boundary (task §3) | How the proof keeps it |
|---|---|
| Live Call / Voice laws (G1.2) | the runtime is G3.2's, unchanged; only the drawing and arrangement inside the 64-pt call line changed |
| C3: Brass = the navigation family only, state-invariant, no geometry granted | Brass only on `#rail .ic` / `#rail .lb` (K03); byte-identical glyphs across states (K18); the geometry is P2's own |
| E1R interaction state | REST = absence; PRESSED = ground (fixed: the wash now lies under content); FOCUS = detached perimeter; SELECTED = marker + weight; DISABLED = one ink; never Brass, never colour alone |
| Timeline / temporal navigation | TM and PTC untouched; 48-pt step (K06); 44-pt band (K07); 1:1 finger (K11); no pulse, ghost or trail (K13, K14); Moment(LH) ≠ Live (K10); logical RTL (K20); the G3 line placement unchanged |
| Analysis appearance | the Analysis stays dark under every appearance (G3 amendment; board 12); non-Analysis follows the resolved appearance (P1 §12). No setting is added |

## 4. Product Owner decisions consumed as direction (task §4)

These decisions were not reopened:
- the hybrid system: signature family + curated utility family + a QANDEEL motion layer;
- the Call Rail direction;
- the Temporal Spine + Aperture direction;
- Open Geometry Hybrid;
- Calm State Morphing;
- a small owned family.

The variants (3 rails, 3 spines, 2 nuances) all sit inside those decisions.

## 5. What this package does not do (task §23)

It does not:
- merge;
- mark P2 closed;
- change a frozen semantic;
- implement production icons or screens;
- touch Voice runtime or a provider;
- redesign the world or the Conversation;
- change the Timeline state machine, the 48-pt step or the 44-pt band;
- draw a fake waveform;
- use Brass as an accent;
- use Hugeicons Pro or any paid asset;
- add or upgrade a package;
- copy competitor morphology;
- build a circle-button dock or a slider thumb;
- draw a literal-Q dock;
- edit a historical proof package.

The G3.2 package under `canonical-artifacts/product-proofs/g3/g3.2/` is **read, not modified**. This package forked a
copy of its source.

## 6. Why no backlog item or locator changed

This is not a closure task (task §24: "do not mutate backlog lifecycle merely to make a closure gate pass"). The backlog
was read, and no backlog item names P2 as its owner.

Two findings are candidates for a later record, but P2-A does not admit them. Their disposition belongs to the P2-B
closure, or to the owners named:
- **F-P2-02:** the Live act covering the Track's end, a T-11 / T-12 implementation composition item;
- **F-P2-01:** a proof-harness truncation that the frozen T-06 contract already forbids.

`QANDEEL_CURRENT_STATE.md` and the roadmap still correctly say P2 is the current track with no closure. They will be
synchronised when P2 closes.
