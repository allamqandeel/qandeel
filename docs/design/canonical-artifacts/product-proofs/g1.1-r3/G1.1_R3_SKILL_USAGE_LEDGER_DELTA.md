# G1.1-R3 — Skill Usage Ledger Delta

This covers the R3 run only. Every Skill below was **opened and read again in this run** (2026-09-23), so none of it is inherited from the G1.1 or A1 ledgers. The hash is the first 16 hex digits of the file's SHA-256 at read time. Check **R3-13** re-hashes every cited file on this host, and planted-hash probe P-R3-13 fails that check.

Paths: `~` is the user profile; `<repo>` is `E:\QANDEEL\QANDEEL PROJECT`.

## 1. Enumeration of the Skills available on this host

These were enumerated by searching `~/.claude/skills`, `<repo>/.claude/skills` and `~/.claude/plugins` for `SKILL.md`.

- **Design, UI and critique:** `apple-design`, `design-critique`, `emil-design-eng`, `frontend-design` (repo and plugin copies), `impeccable` (with `reference/craft-floor.md`), `ui-ux-pro-max` 2.13.0, `prototype`, `user-research`.
- **Arabic:** `designing-arabic-frontends` and `writing-eloquent-arabic`. The repo copies and the `sibawayh` 0.1.0 plugin copies have identical hashes.
- **Accessibility:** `fixing-accessibility`.
- **Motion:** `animate`, `animate-expo`, `animation-vocabulary`, `find-animation-opportunities`, `improve-animations`, `review-animations` (user-invoked only), `pulsar-haptics`.
- **React Native and navigation:**
  - `react-navigation` (references: bottom-tabs, native-bottom-tabs, form-sheet, header, stacks, safe-areas)
  - `react-native-best-practices` (references: animations, audio, gestures, rich-text, svg, jsi, multithreading, on-device-ai, worklets bundle mode)
  - `expo-horizon`, `upgrading-react-native`, `react-native-brownfield-migration`, `assess-react-native-migration`, `create-react-native-library`, `react-native-tv-best-practices`, `rnrepo`, `radon-mcp`
- **Media and real-time:** `fishjam` (and references), `moq-kit`, `react-native-moq`.
- **Unrelated to this revision:** `typegpu`, `github-actions`, `detour/*`, and the claude-plugins-official tooling Skills (plugin-dev, mcp-server-dev, hookify, skill-creator, session-report, receipts, project-artifact, math-olympiad, cwc-makers, discord / imessage / telegram, claude-security, claude-md-management, claude-code-setup, playground, example-plugin).

## 2. Skills re-read and applied

| Skill | Path | SHA-256 (16) | Section / principle re-read | Concrete consequence in R3 | Evidence |
|---|---|---|---|---|---|
| `designing-arabic-frontends` | `<repo>/.claude/skills/designing-arabic-frontends/SKILL.md` | `ab70493bf1b15461` | §4: logical properties resolve per element; `<bdi>` for embedded tokens; set `dir` on inline, not block. §6: mirror by meaning; never mirror media or clocks. §5: no letter-spacing. | Arabic USER = RIGHT is implemented as the logical START edge, so English LEFT is the same rule. The turn container inherits the phone's direction and only the paragraph gets `dir`. The opener's `{display_name}` is isolated in `<bdi>`. Replay, handset and play glyphs are never mirrored, and media rows stay LTR. Board captions isolate every «…» run. | R3-02, R3-03, R3-01b, B10, B02; `proof/conversation/R3-03-mixed-language-bidi.png` |
| `fixing-accessibility` | `<repo>/.claude/skills/fixing-accessibility/SKILL.md` | `549261e8a53b53a1` | §1: names for icon-only buttons. §2: Escape closes overlays. §3: restore focus to the trigger. §6: `aria-expanded` / `aria-controls`. §7: disabled states must not rely on colour alone. | Every mode control and Replay has a name. Replay carries `aria-haspopup`, `aria-expanded` and `aria-controls`. Escape closes the entry and returns focus to Replay, and the preview returns focus to its trigger. Mute uses `aria-pressed` plus a glyph shape change. «معاينة» is `aria-disabled` and dimmed, and the count text says why. | B06, B07, R3-13a, R3-12b; `proof/accessibility/R3-13-accessibility-and-stress.png` |
| `design-critique` | `<repo>/.claude/skills/design-critique/SKILL.md` | `3a4f260eb9f60b43` | First impression (2 s); usability ("unnecessary steps"); consistency ("similar elements behave similarly"). | The glance row judged the speaker sides first. The Replay entry was reduced to one ASIDE with two choices and no extra step. Voice notes reuse the text turn's side and material, because similar elements behave similarly. | `proof/conversation/R3-02-speaker-sides-arabic-english.png` (glance row); R3-04 |
| `apple-design` | `<repo>/.claude/skills/apple-design/SKILL.md` | `11840b24a11d7f94` | §7: spatial consistency, anchored origins. §12: dim to focus, separate to keep flow. §14: Reduced Motion means gentler, not none. | The Replay entry originates at its trigger, with no scrim because it keeps the flow. The preview dims because it is a bounded task. Under Reduced Motion the depth change is a 140 ms resolve without travel, and state and controls are identical. | R3-13a, R3-M1; `proof/motion/R3-12-reduced-motion-parity.png` |
| `animate-expo` | `~/.claude/skills/animate-expo/SKILL.md` | `2121fc72ffa47649` | Operating Posture; §1 "should this animate at all?"; §2 name the purpose; §9 reduced motion; Never Ship. | Only three things move, each for **spatial consistency**: the Live Call depth change, the line mode change and the ASIDE. Mute, a voice note landing and the call record do not animate. There is no new duration or curve, and no loop on the call. | A06, R3-M1 |
| `react-navigation` | `~/.claude/skills/react-navigation/SKILL.md` | `f127331ac68f2966` | Problem → Skill mapping for stacks, tabs, sheets and headers. | Neither Replay nor Live Call is a tab. The rail remains the three Worlds (A02). Header-level actions are kept in the upper chrome, not a tab. | A02, R3-09 |
| `react-navigation` › form-sheet | `~/.claude/skills/react-navigation/references/form-sheet.md` | `b1aa00832f512ff6` | `presentation: 'formSheet'` for secondary flows over the current screen; `sheetAllowedDetents`. | The Replay **preview** maps to a form sheet once a route tree exists. The **entry** is not a sheet: it is an ASIDE anchored to its trigger. G1.1 found that `_layout.tsx` forbids a product route tree today, so both stay ProductRoot state (report §H). | Report §H |
| `react-native-best-practices` › audio | `~/.claude/skills/react-native-best-practices/references/audio/SKILL.md` | `8e3d4d7b87ee7455` | Critical rules: one `AudioContext`, one `AudioRecorder`, suspend when idle, mutate typed arrays for visualisation. | Voice Note and Live Call use **one** level source, and the line draws whichever is live. The RN mapping requires one recorder and one context shared by both modes. The waveform is static per note, so no per-frame allocation is needed. | Report §H; `src/runtime.js` `liveLevel` |
| `writing-eloquent-arabic` | `<repo>/.claude/skills/writing-eloquent-arabic/SKILL.md` | `6a673bd773a9245b` | Register table (warm vs formal); "Eloquent ≠ ornate"; weak-connector and calque modes. | The locked opener is **reproduced, not polished**. The Skill's own advice to rewrite is set aside because R3 §2 locks the wording. New proof copy uses masdar control labels («إلغاء التسجيل», «معاينة», «كتم الميكروفون»). The part hint avoids a gendered imperative. | R3-01a, A07 |
| `impeccable` › craft-floor | `~/.claude/skills/impeccable/reference/craft-floor.md` | `96e2e6bd4fcf9a2c` | Verify: contrast, states, copy. Refuse: "cards are the lazy container", "a modal for a task that needs neither interruption nor protected focus". | The Replay entry is not a modal, so it has no scrim. The slab is not a card. Every new state (recording, muted, part empty or chosen, zero shared worlds) is designed rather than left blank. | A08, R3-05, R3-11b |
| `ui-ux-pro-max` › quick-reference | `~/.claude/plugins/cache/ui-ux-pro-max-skill/ui-ux-pro-max/2.13.0/.claude/skills/ui-ux-pro-max/references/quick-reference.md` | `0609bc7c89dacb40` | §1 `aria-labels`; §2 `touch-target-size` (44 × 44) and `touch-spacing` (≥ 8 px gap). | The two mode controls sit 8 px apart (end 14 / end 66) rather than the 4 px a tighter pack would give. Every target is ≥ 44 × 44 at 100% and 200% text. | B08; `proof/modes/R3-05-writing-and-the-three-ways-of-talking.png` |

## 3. Read and set aside

| Skill | Why set aside |
|---|---|
| `review-animations` | User-invoked only (host memory); not self-invoked. |
| `pulsar-haptics` | A haptic on call start or end belongs on a device. There is none on a web proof, and none is claimed. |
| `fishjam`, `moq-kit`, `react-native-moq` | Real-time media transport is the Voice / Live Call track's decision. G1.1 has no transport and chooses none (R3 §11). |

## 4. Canon read this run (not Skills)

| Source | What it decided here |
|---|---|
| `docs/replay-runtime-v1.md` §1, §5, §7, §19, §20, §62 | Replay is a MATERIAL_ARTIFACT, never a World. Personal original audio or call is NOT PRODUCIBLE. FULL_SOURCE vs SELECTED_EXCERPT. Chronology cannot be reordered. The open Live Head cannot be frozen. Distribution fails closed. |
| `I-08B3.1-B0R…/docs/B0_SURFACE_ONTOLOGY.md` §3 and `I-08B3.1-B4R…/docs/B4_CANONICAL_SURFACE_SPEC.md` §2–3 | The B4 reconciliation (the slab fits no role); the Replay entry is an ASIDE and the preview a PASSAGE. |
| `docs/design/phase-vi/vi-01-bilingual-product-language/QANDEEL_VI01_TERMINOLOGY_MATRIX.md` S02, S04, A11, V01–V07 | Voice and call terms. The **conflict** that R3's «المحادثة» / «تحليل المحادثة» overrides VI-01's APPROVED S02 «الكلام» and A11. |
| `database/migrations/0001`, `0064`, `0111` (via the canon search) | No display-name source; Personal units are TEXT only. |
