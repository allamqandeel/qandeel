# G1.2 — Skill Usage Ledger

The mandatory Skill Gate (brief §11). Every Skill below was **opened and read in this run** (2026-09-23); nothing is
inherited from the G1.1 ledgers. The hash is the first 16 hex digits of the file's SHA-256 at read time. Check **G12-SK**
re-hashes every cited file on this host, and requires every relevant enumerated Skill to be accounted for. Probe
`P-G12-SK` plants a wrong hash and is rejected.

Paths: `~` is the user profile; `<repo>` is `E:\QANDEEL\QANDEEL PROJECT`.

## 1. Enumeration — the Skills actually on this host

Enumerated by listing every `SKILL.md` under `~/.claude/skills`, `<repo>/.claude/skills` and `~/.claude/plugins`
(80 files).

| Category (brief §11) | Skills found |
|---|---|
| Product / interaction design, design critique | `design-critique`, `apple-design`, `emil-design-eng` (repo and user copies, identical hash), `impeccable` (+ `reference/craft-floor.md`, `audit.md`, `critique.md`), `ui-ux-pro-max` 2.13.0 (+ `pro-rules.md`), `frontend-design` (repo copy; the plugin copy differs), `prototype` (repo and user copies, identical), `user-research` |
| Mobile UI, Expo / React Native | `react-native-best-practices` (+ references `audio`, `animations`, `on-device-ai`, `gestures`, `svg`, `rich-text`, `jsi`, `multithreading`, `enable-worklets-bundle-mode`), `animate-expo`, `react-navigation`, `expo-horizon`, `create-react-native-library`, `rnrepo`, `radon-mcp`, `upgrading-react-native`, `react-native-brownfield-migration`, `assess-react-native-migration`, `react-native-tv-best-practices` |
| Voice / audio / real-time media | `react-native-best-practices` › `audio` (+ `audio.md`, `recording.md`, `system-and-notifications.md`, `playback.md`, `effects-and-analysis.md`, `worklets.md`), `fishjam` (+ `platform`, `react-native-client` and its `foreground-service`, `callkit`, `audio-output`, `permissions` references), `moq-kit`, `react-native-moq`, `react-native-best-practices` › `on-device-ai` (+ `speech.md`, VAD) |
| Motion | `animate`, `animate-expo`, `animation-vocabulary`, `find-animation-opportunities`, `improve-animations`, `review-animations`, `react-native-best-practices` › `animations` (+ `animations-performance.md`) |
| Accessibility | `fixing-accessibility`; `apple-design` §14; `ui-ux-pro-max` › pro-rules |
| Arabic / RTL / bidi, Product copy | `designing-arabic-frontends`, `writing-eloquent-arabic` (repo copies; the `sibawayh` 0.1.0 plugin copies are identical) |
| Navigation | `react-navigation` |
| Haptics | `pulsar-haptics` |
| Unrelated to G1.2 | `typegpu`, `github-actions`, `detour/*`, and the claude-plugins-official tooling Skills (plugin-dev ×7, mcp-server-dev ×3, hookify, skill-creator, session-report, receipts, project-artifact, math-olympiad, cwc-makers ×2, discord / imessage / telegram ×6, claude-security, claude-md-management, claude-code-setup, playground, example-plugin ×2) |

## 2. Skills read and APPLIED — section, concrete consequence, evidence

| Skill | Path | SHA-256 (16) | Section / principle read | Concrete consequence in G1.2 | Evidence |
|---|---|---|---|---|---|
| `react-native-best-practices` | `~/.claude/skills/react-native-best-practices/SKILL.md` | `2cafec6f98199a23` | Router: "MUST USE before … ANY code in a React Native or Expo project"; sub-skill map | The production mapping is written against the New Architecture sub-skills (audio, animations), not generic web advice. | Compliance note §4 |
| `react-native-best-practices` › audio | `~/.claude/skills/react-native-best-practices/references/audio/SKILL.md` | `8e3d4d7b87ee7455` | Critical rules: one `AudioContext`, one `AudioRecorder`, **suspend when idle** ("a running AudioContext … prevents the lock screen from showing a paused state") | Production rule: one capture path for Voice Note and Live Call, and **no audio session alive outside a call** — the same rule as the store-safety principle "background audio only for the actual call". | Compliance note §1.3–1.4; dependency note §3 |
| `react-native-best-practices` › audio › audio.md | `~/.claude/skills/react-native-best-practices/references/audio/audio.md` | `c00e53deaa1f3f2e` | Decision tree: "simple file playback … → Consider Expo Audio"; everything real-time → a native audio graph | Grounds §4's conclusion: `expo-audio` fits at most the foreground Voice Note; the Live Call needs a real-time native path. | Compliance note §4 |
| `react-native-best-practices` › audio › recording.md | `~/.claude/skills/react-native-best-practices/references/audio/recording.md` | `b1935fb357f62ce0` | "Background Recording" plugin options (`iosBackgroundMode`, `androidForegroundService`, `androidFSTypes: ["microphone"]`) | Recorded as a **risk**: audio libraries ship background-capture switches, and each must be set deliberately. Voice Notes get **no** background capture (`voiceNoteBackgroundCapture: false`, **R-VN**). | Compliance note §4, §9 R1, §11 |
| `react-native-best-practices` › audio › system-and-notifications.md | `~/.claude/skills/react-native-best-practices/references/audio/system-and-notifications.md` | `2b0d29e58f6042b3` | Session categories (`playAndRecord` "for voice chat"), modes (`voiceChat`), options (`allowBluetoothHFP`, `defaultToSpeaker`); interruption began / ended + `shouldResume`; route changes; "Throws … if NSMicrophoneUsageDescription is missing" | Cross-checked against Apple's own pages before use (§1.2, §1.6, §1.7). The "held / interrupted by another call" state is surfaced as an **undesigned Product state** (G12-J6) instead of silently mapped to reconnecting. | Compliance note §1.2, §1.6, §8 |
| `react-native-best-practices` › animations | `~/.claude/skills/react-native-best-practices/references/animations/SKILL.md` | `cbf41937f716feb6` | Reanimated 4 rules (`scheduleOnRN`, never `runOnJS`) | The production mapping of the prototype's one-clock motion is Reanimated on the UI runtime. The call's level line is a shared value mutated in place (audio rule "mutate typed arrays"), never a React re-render per sample. | Report §M |
| `react-native-best-practices` › animations › performance | `~/.claude/skills/react-native-best-practices/references/animations/animations-performance.md` | `7e65406d6dc1d09c` | `useReducedMotion` "returns true if … enabled **at app start**. Does not update at runtime" | A call lasts minutes, and the reader may change the setting mid-call. Production must use `ReducedMotionConfig` / per-animation `ReduceMotion.System`, not a start-time snapshot. | Report §M, §J |
| `react-native-best-practices` › on-device-ai | `~/.claude/skills/react-native-best-practices/references/on-device-ai/SKILL.md` | `10570525ac88f34d` | STT / VAD hooks; "Audio for speech-to-text and VAD must be 16 kHz mono" | VAD exists as a mechanism, which is why the live label is the mechanical «الميكروفون شغّال» (microphone on), never «بسمعك» (a comprehension claim, VI-01 §14.1). An on-device STT would be the only local source of a transcript — G12-J2 stays a runtime decision. | `lc-03`; dependency note §1 |
| `react-native-best-practices` › on-device-ai › speech.md | `~/.claude/skills/react-native-best-practices/references/on-device-ai/speech.md` | `ba1e3aa56e1b1a3a` | `useVAD` — segments of detected speech | Supports the barge-in candidate's feasibility claim only as "a detection mechanism exists"; its semantics stay an OPEN RUNTIME DEPENDENCY. | `lc-09`; dependency note §2 |
| `fishjam` | `~/.claude/skills/fishjam/SKILL.md` | `dba4eb81c9ac54d4` | Router: production = backend + client; tokens never in the client | The recommendation keeps provider secrets server-side (AGENTS.md §5) and the transport behind the Voice Adapter. | Compliance note §4 |
| `fishjam` › platform | `~/.claude/skills/fishjam/references/platform/SKILL.md` | `4c0fc5e352b80eaa` | Audio-only rooms, peers, the auth flow | Confirms a two-way audio-only transport class exists that fits the Voice Adapter; recorded as a candidate only, **not chosen**. | Compliance note §4 |
| `fishjam` › react-native-client | `~/.claude/skills/fishjam/references/react-native-client/SKILL.md` | `8b83dede86648302` | Mobile delta: permissions, `useForegroundService`, `useCallKit`, `useAudioOutput`; "Foreground service is mandatory for backgrounded streaming on Android" | The production architecture separates **call lifecycle** (CallKit / Telecom) from **transport**. A candidate SDK can supply both, but only the OS-documented rules decide the declarations. | Compliance note §4 |
| `fishjam` › foreground-service | `~/.claude/skills/fishjam/references/react-native-client/foreground-service.md` | `b6cd4f878b0a802c` | Android FGS tied to the call screen's lifecycle; "enable PiP support (which writes `audio` as a side-effect)" | The FGS lives **exactly as long as the call**. The PiP-as-a-side-door pattern is **explicitly rejected**: `audio` is declared directly and only for the call. | Compliance note §1.4, §2.2 |
| `fishjam` › callkit | `~/.claude/skills/fishjam/references/react-native-client/callkit.md` | `39da1bcce3da4a05` | Outbound CallKit pattern; "Always handle the ended event"; inbound needs PushKit | Every system end path must end the SAME call (compliance note §7). PushKit is kept out of scope, because there are no incoming calls (brief §7). | Compliance note §1.5, §7 |
| `fishjam` › audio-output | `~/.claude/skills/fishjam/references/react-native-client/audio-output.md` | `d0655440fc1eb3a5` | Default routing heuristics; override only for an explicit speaker toggle | The route control is ONE speaker toggle (`aria-pressed`, filled / outline shape), not a route menu. Bluetooth and other routes are the OS picker's. | `lc-07`; **G12-Q11b** |
| `fishjam` › permissions | `~/.claude/skills/fishjam/references/react-native-client/permissions.md` | `f43201ec5b2c5c85` | Request before init; iOS re-request silently returns the previous state → Settings deep link; "be honest and specific" in usage strings | The denied notice carries «فتح الإعدادات» (Settings route), a later intent never re-prompts, and the proposed purpose string names both features and "only while they are running". | `pf-01`, `vn-01`; **G12-P1** |
| `animate-expo` | `~/.claude/skills/animate-expo/SKILL.md` | `2121fc72ffa47649` | §1 frequency gate; §2 name the purpose; §8 haptics "Never the only feedback"; §9 reduced motion "fewer and gentler"; Never Ship | Gate results. **Animate:** the depth change on call start (spatial consistency) and the line becoming the call line (state indication, the G1.1 220 ms). **Do NOT animate:** the OS prompt and lock surfaces (OS-owned), the notice, a voice note landing, the call state words, mute, the restore on return. The restore is the SAME surface, so nothing moves. No new duration. | **G12-06**, **G12-M1** |
| `animate` | `~/.claude/skills/animate/SKILL.md` | `f6317335da2662e9` | Build sequence; "Never ease-in"; "Transitions, not keyframes, for anything triggered rapidly" | The one T-10 curve, interruptible from the presentation value (the prototype's `get()`); no keyframes anywhere. | **G12-06** |
| `animation-vocabulary` | `~/.claude/skills/animation-vocabulary/SKILL.md` | `d718b48fe3c78988` | Glossary: *Continuity transition*, *Idle animation*, *Pulse* | Names what is refused: no *pulse* on the call, no *idle animation* on QANDEEL. The call-start depth change is a *continuity transition*. | Report §M |
| `find-animation-opportunities` | `~/.claude/skills/find-animation-opportunities/SKILL.md` | `91c1243164057fbf` | "Expect to reject most candidates"; rejected-candidates list | Rejected: a breathing "call active" glow; a QANDEEL speaking orb; a waveform of QANDEEL's voice; an animated Analysis that reacts to sound; a slide-in on the notice. | Report §M |
| `improve-animations` | `~/.claude/skills/improve-animations/SKILL.md` | `68f17bbc4671593d` | Audit categories: purpose & frequency, interruptibility, accessibility | Self-audit of the inherited motion: restore-on-return must not replay the call-start transition (it would lie about continuity). The runtime writes nothing on return; the prototype's `toForeground` only re-commits. | **G12-Q4Q5** |
| `emil-design-eng` | `<repo>/.claude/skills/emil-design-eng/SKILL.md` | `e71de849347050c2` | "Unseen details compound"; "Handle edge cases invisibly"; frequency rules; asymmetric timing | The call's invisible edge cases are designed, not left blank: a capture stopped by leaving becomes a draft; a recovered call keeps its id; a commit during a network gap never fakes an Analysis change. | **G12-VN**, **G12-AN** |
| `apple-design` | `<repo>/.claude/skills/apple-design/SKILL.md` | `11840b24a11d7f94` | §16 Principles 2026: *Agency* (control), *Responsibility* ("Privacy: ask at the right moment, only for what's needed"), *Familiarity*; §14 Reduced Motion; §13 multimodal "Utility" | The permission is asked at the moment of intent, not at launch. The handset glyph is kept (familiar), while the call SURFACE is QANDEEL's Analysis, not a telecom screen. Every end path is the reader's. | `vn-01`, `pf-02`; **G12-P1** |
| `fixing-accessibility` | `<repo>/.claude/skills/fixing-accessibility/SKILL.md` | `549261e8a53b53a1` | §1 names for icon-only buttons; §3 focus return; §6 announcements (`aria-live`, "toasts must not be the only way"); §7 states not by colour alone | Every call control is named. Mute and speaker expose `aria-pressed` and change shape. Call phases are announced once per phase change, not per level sample. The failure notice persists (not a toast). Escape never ends a call. | **G12-Q11a–c** |
| `designing-arabic-frontends` | `<repo>/.claude/skills/designing-arabic-frontends/SKILL.md` | `ab70493bf1b15461` | §4 two direction contexts, `<bdi>`; §6 "Never mirror … media playback"; §5 no letter-spacing | The voice-note media row reads left-to-right in both scripts inside a slab that stays on the reader's edge. The OS prompt's Arabic purpose string is its own RTL paragraph inside an LTR schematic (fixed after a visual defect). | `vn-05`, `bi-02`, `vn-01`; **G12-BI** |
| `writing-eloquent-arabic` | `<repo>/.claude/skills/writing-eloquent-arabic/SKILL.md` | `6a673bd773a9245b` | Register table (formal for errors / consent; warm for greetings); "robotic-tone — frame around the process"; masdar labels | The permission purpose string and notices are neutral T4 («يستخدم قنديل الميكروفون …»). Failures are process-framed («الاتصال انقطع», «المكالمة وقفت»), never a confession. Controls are masdar («إنهاء المكالمة», «كتم الميكروفون», «إلغاء التسجيل»). | **G12-07** |
| `design-critique` | `<repo>/.claude/skills/design-critique/SKILL.md` | `3a4f260eb9f60b43` | First impression; usability "unnecessary steps"; consistency | The critique found two defects, both fixed. **Clipped call words** at 200% and for V06 → the full-width strip. **Speaker state visible only to screen readers** → a filled / outline speaker glyph. It also kept the three ways of talking at two icons on one line (no toolbar). | Report §N; `a11y-03`, `pf-03` |
| `impeccable` | `~/.claude/skills/impeccable/SKILL.md` | `125f732891f3f319` | Mode = **Operate**: "Scanability, consistency, native expectations … outrank expression. Brand lives in precise details." | The call is not a spectacle. Its identity is the Analysis hero and the one-line call, not a new chrome layer. | Report §F |
| `impeccable` › craft-floor | `~/.claude/skills/impeccable/reference/craft-floor.md` | `96e2e6bd4fcf9a2c` | Verify states "hover, disabled, loading, error, empty"; Refuse "a modal for a task that needs neither interruption nor protected focus", "Glass and blur as decoration" | Every call phase is a designed state (connecting / reconnecting / failed / ended / draft). The failure and denial notices are strips, not modals. No glass or glow. | **G12-05**, `pf-*` |
| `impeccable` › audit | `~/.claude/skills/impeccable/reference/audit.md` | `57402ef6fd9d0745` | A11y: "`prefers-reduced-motion` needs an intentional alternative that preserves state change"; responsive "Text scaling: Layouts that break when text size increases" | The 200% text audit, which found and fixed the clipped call words; Reduced Motion keeps every state (the flat level line + words). | `a11y-03/04/09/10` |
| `ui-ux-pro-max` | `~/.claude/plugins/cache/ui-ux-pro-max-skill/ui-ux-pro-max/2.13.0/.claude/skills/ui-ux-pro-max/SKILL.md` | `ea087c341bfb5b23` | Priority table; its data rows for "Voice Recorder & Memo — recording red + waveform blue" and the "Voice-First Multimodal" style's "listening / processing / speaking" indicators | Used as a **map of the category default to refuse** (brief §27): no recording red, no waveform accent, no "processing" state (VI-01 bans "thinking"), no AI-purple. | **G12-05**, **G12-07** |
| `ui-ux-pro-max` › pro-rules | `~/.claude/plugins/cache/ui-ux-pro-max-skill/ui-ux-pro-max/2.13.0/.claude/skills/ui-ux-pro-max/references/pro-rules.md` | `d28442d61c310b49` | Touch ≥ 44 pt / 48 dp; "Color is not the only indicator"; "Verified behavior with reduced-motion … and largest system text size" | The third call control (speaker) takes the 118 px slot, keeping 8 px gaps. Checked at 100% and 200%, in Arabic and English. | **G12-Q2**, **G12-Q11b** |
| `react-navigation` | `~/.claude/skills/react-navigation/SKILL.md` | `f127331ac68f2966` | Problem → reference map (stacks, tabs, sheets) | Neither Voice Note nor Live Call is a route or tab (the rail = the three World types). The call is ProductRoot state that survives depth changes, not a pushed call screen, so no stack pop can end it. | **G12-04** |
| `prototype` | `<repo>/.claude/skills/prototype/SKILL.md` | `2ad8401c4deaddb5` | "Never touch production code during exploration"; "Every variant fully works"; isolated surface | The whole proof lives in an untracked work dir; every state is reachable by real acts; no tracked file changed. Variants were not multiplied, because G1.2 is not a divergence task. | **G12-14** |

## 3. Skills read and SET ASIDE — with the reason

| Skill | Path | SHA-256 (16) | Why set aside |
|---|---|---|---|
| `moq-kit` | `~/.claude/skills/moq-kit/SKILL.md` | `d0d5e071e22ed6e0` | Media over QUIC is a *live-streaming* stack (sub-second playback, publishing to a relay); a one-to-one voice call with QANDEEL is not its use case, and "moq-kit is an active preview". Not recommended for the Live Call. |
| `react-native-moq` | `~/.claude/skills/react-native-moq/SKILL.md` | `235709c3c1be94cd` | As above; its own rule 9 ("an idle iOS microphone capture holds the audio session in playAndRecord") is noted as a general hazard, now compliance-note R1. |
| `pulsar-haptics` | `~/.claude/skills/pulsar-haptics/SKILL.md` | `fb58618ee4572ff6` | Stop condition: "No Pulsar evidence after inspection: stop using this skill." Its principle "Keep every flow usable without haptics; pair critical states with visible … feedback" is honoured anyway: no call state depends on haptics. |
| `frontend-design` | `<repo>/.claude/skills/frontend-design/SKILL.md` | `1608ea77fbb6fc30` | Its "take one real aesthetic risk" posture belongs to G2 (the Living Analysis). G1.2 must not redesign the visual world. Its writing section ("Errors … never vague about what happened") is consistent with VI-01 and applied through it. |
| `user-research` | `<repo>/.claude/skills/user-research/SKILL.md` | `fa18fc13d0f44aa8` | No research is run in a proof task. A usability test of the restore rule and the three-way control is recommended as a post-review step (report §P). |
| `expo-horizon` | `~/.claude/skills/expo-horizon/SKILL.md` | `45a2b58e17824ccd` | Meta Quest / Horizon OS migration — not a QANDEEL target. |
| `create-react-native-library` | `~/.claude/skills/create-react-native-library/SKILL.md` | `72fc31f0dbf032ce` | Relevant only when the native call module is built. The CNG hierarchy (config plugin / Expo Module) is cited instead. |
| `rnrepo` | `~/.claude/skills/rnrepo/SKILL.md` | `0effa7af402853b4` | Build-time prebuilt artifacts; no build in this task. |
| `radon-mcp` | `~/.claude/skills/radon-mcp/SKILL.md` | `1391deacb9d4ab04` | Live RN-app inspection; there is no running RN app in this proof. |

**Not invoked, and why.** `review-animations` is gated to explicit user invocation on this host (the Skill tool refuses
it and forbids replicating its workflow). `animate-expo` and `react-native-best-practices` › animations cover the same
ground and were applied. `impeccable › critique.md` was opened, but only its assessment headings were read. The critique
used is `design-critique`, whose output is in report §N.

**Mobile / RN Skills not relevant to G1.2:** `upgrading-react-native`, `react-native-brownfield-migration`,
`assess-react-native-migration` and `react-native-tv-best-practices` (no upgrade, migration or TV work), plus the
`gestures`, `svg`, `rich-text`, `jsi`, `multithreading` and `enable-worklets-bundle-mode` sub-skills (no such work in a
proof).

## 4. Gate result

Every Skill relevant to G1.2's categories was enumerated, read, and either applied with a consequence or set aside with
a reason. **G1.2 SKILL GATE = PASS** (subject to check G12-SK re-hashing the cited files).
