# I-08B3.1-F1 — VALIDATION RESULTS

**GENERATED from `data/F1_VALIDATION.json` by `tools/f1-verify.mjs`.**

**59/59 checks pass. 59/59 probes reject the input built to make them fail.**

> Every guard is fed an input that must make it fail. **A check that cannot fail is a
> sentence.** A probe that stops rejecting is itself a failure.

| id | check | result | probe |
|---|---|---|---|
| **V-01** | every vendored file matches the sealed package it was taken from | **PASS** | rejects: a published file whose recorded sha256 is altered |
| **D-01** | with NO accessibility setting on, every inherited role resolves to E1's literal by E1's route | **PASS** | rejects: the same comparison with one role re-pointed at another rung |
| **D-02** | DEFAULT ACCESSIBILITY-OVERRIDE ISOLATION — with every accessibility override file emptied, the DEFAULT document is byte-identical | **PASS** | rejects: the same hashes with one byte of the ablated document changed |
| **D-04** | D-02 states, in the shipped record, the claim it does NOT support | **PASS** | rejects: a record that carries no doesNotProve field at all |
| **D-03** | every accessibility modifier's DEFAULT context declares zero token values | **PASS** | rejects: the increased-contrast context, which is NOT a default |
| **S-01** | no accessibility override reaches a COLOUR under the identity material, the Light, a state ink or a status ink | **PASS** | rejects: a synthetic colour-typed override of qandeel.state.focus.indicator |
| **S-02** | F1 introduces no colour literal except the computed opaque scrim substitution | **PASS** | rejects: the same scan run over E1's own dark appearance file, which does introduce literals |
| **C-01** | the World fill and the functional Surface do NOT move under increased contrast | **PASS** | rejects: pure black as a ground |
| **C-02** | Living Brass, QANDEEL Light, the error ink and the disabled ink are unchanged under increased contrast | **PASS** | rejects: the rest ink, which DOES move |
| **C-03** | every ambient contour reaches at least 3:1 under increased contrast | **PASS** | rejects: the DEFAULT atmosphere at its own alphas |
| **C-04** | the meaning hierarchy WIDENS under increased contrast and does not compress | **PASS** | rejects: the atmosphere raised alone, with the analytical relation left where it was |
| **C-05** | no ambient contour reaches the on-screen strength of the weakest analytical object | **PASS** | rejects: a contour lifted to the analytical relation's own contrast |
| **C-06** | the boundary is an existing reading rung and is NOT the availability ink | **PASS** | rejects: the availability ink as a boundary |
| **C-07** | the focus perimeter thickens and the focus INK does not change | **PASS** | rejects: a thickness that did not change |
| **C-08** | the rest-to-unavailable distance widens under increased contrast | **PASS** | rejects: the same ratio with the disabled ink raised too |
| **T-01** | the PASSAGE scrim becomes exactly the value it produced over the World | **PASS** | rejects: the composite over the functional Surface, which is a different value |
| **T-02** | the opaque substitution reproduces the composited value exactly, for every stroke the field draws | **PASS** | rejects: a substitution computed against the functional Surface instead of the World |
| **T-03** | QANDEEL Light keeps its translucency under Reduce Transparency | **PASS** | rejects: a floor of 1, which would make the Light opaque |
| **T-04** | no analytical or identity colour changes under Reduce Transparency | **PASS** | rejects: the scrim, which DOES change |
| **N-01** | hue distinguishes NOTHING in the ambient field — measured, not asserted | **PASS** | rejects: the error ink against the World, a pair that genuinely separates at 8.61:1 |
| **N-02** | every colour-bearing distinction declares a non-colour companion channel | **PASS** | rejects: a distinction with no companion declared |
| **N-03** | the error ink cannot rely on WCAG 1.4.1's lightness escape hatch, so its companion is an obligation | **PASS** | rejects: the error ink against the World, where it does reach 3:1 |
| **X-01** | the combined expression is exactly the two overrides composed — no third set of values exists | **PASS** | rejects: a role that differs in both single-setting expressions |
| **X-02** | ARABIC MUST NOT CLIP and no element is truncated, at any text setting — tested against the current threshold (1.6) | **PASS** | rejects: a row whose measured minimum leading ratio sits below the current threshold |
| **X-03** | Bold Text preserves the 100-unit weight step SELECTED depends on | **PASS** | rejects: a step of 0 |
| **X-04** | every analytical object of every KIND is reachable at every text setting | **PASS** | rejects: a census in which only the topics are present, which is what the pre-F1R scene produced above the escape scale |
| **M-01** | no semantic motion is removed, and every one names a SURVIVING channel that carries it | **PASS** | rejects: a semantic row claiming to be carried by TRAVEL, which is suppressed |
| **M-02** | F1 does not retune the frozen lifecycle | **PASS** | rejects: a shortened fall |
| **M-03** | the runtime default for reduced motion is recorded as the failure mode it is | **PASS** | rejects: a record with no exiting-animation row |
| **W-01** | F1R's own surfaces record D and E as CLOSED / FROZEN, and the untouched vendored language is declared rather than silently contradicted | **PASS** | rejects: the withdrawn README line "I-08B3.1-D2R  QANDEEL Light        freeze candidate" |
| **G-01** | the North Star is recorded as an OPEN obligation owned by G, and F1 claims no spectacle capacity | **PASS** | rejects: a status of PROVEN |
| **G-02** | no surface converts "no token constrains density" into permission to raise it, and the limit is stated | **PASS** | rejects: the withdrawn sentence, standing alone with no retraction near it |
| **G-03** | F1R does not reopen D, and says what would have to be true before anyone did | **PASS** | rejects: a carry-forward that proposes a D change outright |
| **I-01** | every written attribute of the inherited ATMOSPHERE layer equals what D2R's OWN exported functions produce | **PASS** | rejects: the same comparison with one stroke alpha changed to 0.5 |
| **I-02** | the inherited layer of F1's DEFAULT document is pixel-identical to a reference built from D2R's exports ALONE | **PASS** | rejects: the same reference with one contour displaced by one pixel |
| **I-03** | the settled analysis layer's GEOMETRY equals D2R's own sites, element for element | **PASS** | rejects: a comparison in which the element counts differ |
| **I-04** | the inherited-default proof enumerates what it does NOT verify, including the chrome and D2R's own page | **PASS** | rejects: an empty unverified list |
| **A-01** | no renderer or projection file writes a semantic value as a literal — they come from V only | **PASS** | rejects: the same scan run over tools/f1-fixture.mjs, which DOES author them |
| **A-02** | every semantic value the view supplies is preserved, and a value it omits stays omitted | **PASS** | rejects: a view with the insight's epistemic status changed to observed — the projection must follow it, not its old hardcode |
| **A-03** | the synthetic fixture's provenance is stamped into the rendered document and the projection | **PASS** | rejects: a document with no provenance stamp |
| **A-05** | a semantic field V did not supply produces no value AND no announcement — not «غير مُحدَّد», not observed, not hypothesis | **PASS** | rejects: the WITHDRAWN composer with its «غير مُحدَّد» fallback, run over the same unstated row |
| **A-06** | an undisclosed Connection endpoint or Pattern member leaks no raw id and no recovered name, and the broken reference is DETECTED rather than turned into prose | **PASS** | rejects: the WITHDRAWN `disclosed name ?? raw id` resolver over the same broken view, which DOES leak the marker id |
| **A-04** | the token tree carries ACCESSIBLE SEMANTICS = PROJECT(V) as a product-contract token | **PASS** | rejects: the canvas token, which is an implementation strategy and must NOT be the contract |
| **K-01** | every token F1 authors declares a freeze class from the enumerated set | **PASS** | rejects: a token with the class "frozen-forever", which is not in the set |
| **K-02** | every implementation-strategy token names a real product-contract token it serves | **PASS** | rejects: a strategy token whose `serves` names a token that does not exist |
| **K-03** | the library-specific mechanisms are classified as implementation strategy, not as Product truth | **PASS** | rejects: the reduced-motion requirement itself, which IS Product truth and must stay contract |
| **K-04** | every numeric accessibility calibration constant is PRODUCTION DEFAULT and names the requirement it instantiates | **PASS** | rejects: the rule that Bold Text must not erase SELECTED, which is Product truth and must stay contract |
| **K-05** | no Ambient property D2R declares `encodes: null` is promoted into a required semantic accessibility carrier | **PASS** | rejects: the withdrawn token `nonColorCue.depth = level-line-count+scale`, classified product-contract |
| **K-06** | the Arabic Product contract is NO CLIPPING, and the numeric line-height threshold is a production default that serves it | **PASS** | rejects: the numeric threshold classified as an irreversible product contract, which is what F1R shipped |
| **B-01** | the synthetic fixture's field set is never described as the complete semantic model of canonical V, and its scope ships as DATA | **PASS** | rejects: the withdrawn sentence "the complete list of what a projection may carry", standing alone |
| **B-02** | the completeness contract is a product-contract token that records the production-mapping dependency, and an unmapped disclosed field is DETECTED rather than silently dropped | **PASS** | rejects: a view supplying a field the projector has no mapping for — it must be reported, and the shipped fixture must report none |
| **B-03** | every document that quotes the parity matrix describes it as a BOUNDED proof of the tested dimensions | **PASS** | rejects: a document quoting the cell count with no bounding language anywhere in it |
| **R-01** | every analytical object has an engine-computed accessible name, and relationships are stated in words | **PASS** | rejects: a fabricated accessible name |
| **R-02** | traversal order is the DECLARED algorithm, is explicitly non-semantic, and carries no rank | **PASS** | rejects: the withdrawn wording "the Product's own stable identity order" |
| **R-03** | permuting the source array changes neither the traversal order nor one semantic fact | **PASS** | rejects: the same comparison against an order sorted by a fabricated per-object strength |
| **P-01** | the same analytical objects, relationships, temporal truth, epistemic status and actions exist in EVERY expression | **PASS** | rejects: a fabricated cell with a missing object |
| **P-02** | removing ONE object from the rendering fails parity for that object and for no other — TOPIC, CONNECTION, PATTERN and INSIGHT independently | **PASS** | rejects: a planted set covering only three of the four families |
| **P-03** | the truth JSON alone cannot satisfy rendered presence | **PASS** | rejects: a run in which the object was removed from the JSON too, which would prove nothing about the renderer |
| **P-04** | no accessibility expression names fewer analytical objects than the DEFAULT, and every object is named in the projection and in the inspection view | **PASS** | rejects: an expression that lost one name relative to the default |

## What each class of check is for

| class | question it answers |
|---|---|
| **V** | did every inherited byte come from the sealed package it claims? |
| **D** | did F1 weaken the default? *(D-02 is the ablation — the one with teeth)* |
| **S** | can an accessibility override reach a colour it must never reach? |
| **C** | does Increase Contrast raise legibility without inventing importance? |
| **T** | does Reduce Transparency preserve the World? |
| **N** | is colour ever the only required carrier? |
| **X** | do the settings compose, and does text survive ×3.118? |
| **M** | is any semantic motion removed? |
| **R** | does a screen-reader user reach the same truth? |
| **P** | is the analytical content identical in every expression? |
