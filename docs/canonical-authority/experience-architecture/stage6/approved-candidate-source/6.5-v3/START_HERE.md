# START HERE — QANDEEL_STAGE6_5_IMPLEMENTATION_CONTRACT_CANDIDATE_v3

**Stage 6.5 — Implementation Contract + Build Sequencing** · applies `QANDEEL_STAGE6_5_BLOCKER_RESOLUTION_AND_V3_RULING_v1` · carries forward `…ARCHITECTURE_RULING_v1` · 2026-09-03

**Stage 6.5 is NON-CODING and is not declared frozen by this package.** No source modified · no repository file, branch, commit, PR or migration created · working tree and `HEAD` verified unchanged · no deferred OPEN resolved · no Product rule reinterpreted · **implementation is not authorized.**

---

## ✅ Both blockers removed · ⛔ no technical blocker · all 11 stop rules clear

v2 refused to invent the Thread and Live Focus constitution rules. Architecture has supplied both from frozen Stage 0/1 authority that a repository-only inspection could not reach. **They are now executable contracts, not TODOs** — §8A SDM-02 and SDM-04, delta rows 7–16, tasks T-03B1/B2/B3 and T-03D. §36 records both as removed and reports **no technical blocker**: the repository lacks every required substrate, but that is `MISSING / BUILD` — ordinary work with a clear contract — not an inability.

**v2's evidence is not discarded.** Its disproof of `Thread = connected component(competing_hypothesis_ids)` is now *redundant rather than wrong* — relations are formally excluded from constituting focus — and the committed context-activation clause turns out to govern user-owned context binding, a different object from `LF`. **No contradiction exists.**

---

## The correction that changes the most: a Moment is one committed CU

**REV65-08 rejects v2's finalized-exchange mapping.** A Moment is **one committed Conversational Unit**: it belongs to exactly one runtime turn, never crosses a turn boundary, and one turn may yield 0..N. A USER CU is `m` and an ASSISTANT CU is `m+1` — **never merged**, and an exchange is never one Moment merely because it finalizes atomically.

**Targeted inspection confirms the honest classification:** zero `unit` / `segment` / `span` / `utterance` concepts exist in 63 migrations, so the committed-CU substrate is `MISSING / BUILD`, and **the Product rule is not weakened to fit `finalize_conversation_turn`**. `ConversationTurnCompleted` is retained as operational evidence and **demoted** — a turn may complete committing zero CUs, or several.

**§18 re-checks the Timeline rather than assuming it survives.** Constant step holds (`SP` is gapless, so the index *is* the position — now a property of the data, not just the layout). One-Moment traversal survives and becomes finer-grained. A batched multi-CU commit advances `LH` by several `SP` in one event — a *delivery* fact, not a navigation one. `N` grows by the CUs-per-turn factor, which leaves §20B's bounded-reach proof valid as written and more necessary in practice.

## The other four things worth reading first

**1 · Five steps, one transaction (§8A SDM-04, ruling §17).** CU committed → references resolved → Emerging Focus continuity → optional Thread establishment → effective `LF`. Running all five inside the commitment transaction is what makes *exactly one effective `LF` per `SP`* and *no historical rewrite* structural rather than asserted. If the same CU establishes the attended focus as a Thread, `LF` at that `SP` is the new Thread and earlier `SP`s keep the Emerging Focus.

**2 · `LF` has no client-side writer at all.** The most likely way *Live Focus ≠ Inspected Focus* is lost is a Map act quietly setting it — so inspection, Pan, Zoom, contextual navigation and explicit context activation are all **inert** with respect to `LF`, and the per-field authority table makes that executable. `LF = NONE | EmergingFocus(id) | EstablishedThread(id)`, and **v2's nullable `focus_ref` is rejected**: every Emerging Focus carries a stable session-scoped id.

**3 · Geography begins once and cannot move — because no merge path is implemented.** Home is assigned at establishment from then-legitimate world context and guarded immutable. Two established Threads are **never** merged, so `Home(e, t ≥ establishment) = constant` holds by there being nothing to invoke. Establishment is TE-01 / TE-02 / TE-03 with recorded reason codes; **a numeric score in their place is a rejection fixture**, not a guideline.

**4 · R-02 is frozen and integrated**, with the v2 phrase narrowed: a checkpoint whose Session is no longer current is discarded whole and enters the **canonical app/session entry flow** — **never a synthesized `FOLLOW_LIVE`**, because there is no Live edge to follow. On restart: re-read `LH` **and `LF`**, recompute `K(TC)` and `V`, restore no animation. A restart is not an act, writes no `RH`, and is not `Back`-reversible.

## What Stage 6.6 must find supplied

Three **frozen Stage 1 artifacts** must be handed to their tasks as executable specifications. Naming them is the condition under which coding never has to decide semantics — Stage 6.5 does **not** restate, approximate or extend any of them:

| | Input | Consumer |
|---|---|---|
| **INPUT-01** | Stage 1.2 CU boundary + commitment grammar | T-03A1 |
| **INPUT-02** | Stage 1 reference / attention / coreference contract | T-03B1, T-03D |
| **INPUT-03** | TE-01 / TE-02 / TE-03 evidence predicates | T-03B2 |

## Everything the first ruling approved is carried forward untouched

CS-01 and the RN + Expo CNG direction · the four-level escape-hatch policy · `K(TC)` vs the depth-disclosed view `V` and its three states · the AF64-02 presentation-position scale and its 10k/100k proof · the Map accessibility mapping, **now unblocked** · the OPEN-17 seam, now proved against **three** structural guards.

**19 tasks, nothing blocked.** T-03 splits further because constitution, ordering, focus continuity, establishment, binding, entitlement and attention are seven contracts with seven failure modes; the count is not preserved for consistency.

## Contents

| File | What it is |
|---|---|
| `STAGE6_5_IMPLEMENTATION_CONTRACT_CANDIDATE_v3.md` | The candidate — §0 compliance map, §7A inspection record, §8A the six-item Semantic Domain Mapping, §8B reconstructability, §13 the two layers, §18 the CU re-check, §20A/§20B reach, §21A Map accessibility, §26 the seam, §27A frozen R-02, §30 the proofs including P65-02R/08/09/10, §31–§33 the 19-task sequence, §36 blockers and inputs, §43 the v2→v3 change log. |
| `diagrams/S6.5_v3_Implementation_Architecture.png` | Diagram D (SDM outcome), B (the CU commitment transaction + two-layer projection), A (state authority, `LF` producer), C (19-task graph). |
| `SHA256SUMS.txt` | Integrity checksums, POSIX paths, no BOM. |

**Canonical baseline:** `f322112ec5b862a83716bf9d65b4553b06931774`, via the GitHub Contents API (19 sources listed in §7A). The local clone does not contain that commit and was not fetched into.
