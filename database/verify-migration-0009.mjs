import assert from 'node:assert/strict'; import { randomUUID } from 'node:crypto'; import process from 'node:process'; import pg from 'pg';
const {Client}=pg; if(!process.env.DATABASE_URL) throw new Error('DATABASE_URL is required in the ignored local .env file.');
const client=new Client({connectionString:process.env.DATABASE_URL});
async function identity(id){ await client.query('SET LOCAL ROLE authenticated'); await client.query("SELECT set_config('request.jwt.claims',$1,true)",[JSON.stringify({sub:id,role:'authenticated'})]); }
async function rejects(text,values=[]){ await client.query('SAVEPOINT expected_failure'); try{ await assert.rejects(client.query(text,values)); }finally{ await client.query('ROLLBACK TO SAVEPOINT expected_failure'); await client.query('RELEASE SAVEPOINT expected_failure'); } }
async function verifyCanonicalContract(){
  const contract=(await client.query(`SELECT to_regclass('public.him_metric_definitions') IS NOT NULL definitions_present,
    to_regclass('public.him_metric_snapshots') IS NOT NULL snapshots_present,
    p.prosecdef security_definer,p.provolatile volatility,pg_get_function_result(p.oid) result_identity,
    pg_get_functiondef(p.oid) definition,
    has_function_privilege('authenticated',p.oid,'EXECUTE') authenticated_execute,
    has_function_privilege('anon',p.oid,'EXECUTE') anon_execute
   FROM pg_proc p WHERE p.oid=to_regprocedure('public.validate_him_metric_dependencies()')`)).rows[0];
  assert.ok(contract,'Schema contract mismatch: validate_him_metric_dependencies() is absent.');
  assert.equal(contract.definitions_present,true,'Schema contract mismatch: him_metric_definitions is absent.'); assert.equal(contract.snapshots_present,true,'Schema contract mismatch: him_metric_snapshots is absent.');
  assert.equal(contract.security_definer,false,'Schema contract mismatch: dependency validator authority changed.'); assert.equal(contract.volatility,'v','Schema contract mismatch: dependency validator volatility changed.'); assert.equal(contract.result_identity,'trigger','Schema contract mismatch: dependency validator result changed.');
  assert.match(contract.definition,/Unresolved HIM metric dependency/u,'Schema contract mismatch: unresolved-dependency guard changed.'); assert.match(contract.definition,/Cyclic HIM metric dependency/u,'Schema contract mismatch: cyclic-dependency guard changed.');
  assert.equal(contract.authenticated_execute,false,'Schema contract mismatch: authenticated can execute dependency validator.'); assert.equal(contract.anon_execute,false,'Schema contract mismatch: anon can execute dependency validator.');
  const trigger=(await client.query(`SELECT t.tgdeferrable,t.tginitdeferred,t.tgenabled,pg_get_triggerdef(t.oid) definition
    FROM pg_trigger t WHERE t.tgrelid='public.him_metric_definitions'::regclass AND t.tgname='him_metric_dependencies_valid' AND NOT t.tgisinternal`)).rows[0];
  assert.ok(trigger,'Schema contract mismatch: HIM dependency constraint trigger is absent.'); assert.equal(trigger.tgdeferrable,true,'Schema contract mismatch: dependency trigger is not deferrable.'); assert.equal(trigger.tginitdeferred,true,'Schema contract mismatch: dependency trigger is not initially deferred.'); assert.equal(trigger.tgenabled,'O','Schema contract mismatch: dependency trigger is disabled.'); assert.match(trigger.definition,/validate_him_metric_dependencies\(\)/u,'Schema contract mismatch: dependency trigger binding changed.');
}
async function main(){ await client.connect(); try{
  await verifyCanonicalContract();
  await client.query('BEGIN'); try{
    const user=randomUUID(),other=randomUUID(),memory=randomUUID(),otherMemory=randomUUID(),decision=randomUUID(),otherDecision=randomUUID(); await client.query('RESET ROLE');
    await client.query('INSERT INTO auth.users(id) VALUES($1),($2)',[user,other]);
    await client.query("INSERT INTO public.memories(id,user_id,scope,type,content,source,confidence,importance,status) VALUES($1,$3,'USER','PERSONAL_FACT','owned','USER_STATED',.9,.5,'ACTIVE'),($2,$4,'USER','PERSONAL_FACT','other','USER_STATED',.9,.5,'ACTIVE')",[memory,otherMemory,user,other]);
    await client.query(`INSERT INTO public.him_metric_definitions(metric_key,definition_version,canonical_name,canonical_definition,canonical_source,hif_owner,semantic_mapping_status,semantic_type,calculation_status,scale_reference,valid_context_kinds,required_input_contract,confidence_requirement_reference)
      VALUES('test.synthetic.zero',1,'Test only','Verifier-only canonical fixture','verifier','HSE','RESOLVED','STATE','CALIBRATED','fixture-scale',ARRAY['DECISION'],'fixture-input','future-confidence-reference')`);
    await client.query(`INSERT INTO public.him_calculation_models(id,model_id,model_version,target_metric_key,target_definition_version,lifecycle,environment,canonical_owner,canonical_source,method_type,scale_contract_reference,required_input_contract,required_evidence_contract,supported_context_kinds,missing_data_behavior,contradiction_behavior,confidence_contract,implementation_id,created_at,versioned_at)
      VALUES($1,'test.synthetic.zero',1,'test.synthetic.zero',1,'CALIBRATED','TEST_ONLY','QANDEEL_VERIFIER','verifier','DETERMINISTIC_TEST','fixture-scale','{}'::jsonb,'VERIFIER_ONLY',ARRAY['DECISION'],'UNASSESSED','UNASSESSED_PRESERVE_CONFLICT','UNRESOLVED_METRIC_CONFIDENCE','verify-migration-0009',CURRENT_TIMESTAMP,CURRENT_TIMESTAMP)`,[randomUUID()]);
    const zeroResult=(await client.query(`INSERT INTO public.him_calculation_results(id,user_id,metric_key,definition_version,model_id,model_version,context_kind,context_id,result_state,numeric_value,missing_input_keys,contradiction_state,supporting_evidence_refs,contradictory_evidence_refs,provenance,confidence_state,confidence_reference,trace_id,update_reason)
      VALUES($1,$2,'test.synthetic.zero',1,'test.synthetic.zero',1,'DECISION',$3,'ASSESSED',0,ARRAY[]::text[],'NONE',ARRAY[]::text[],ARRAY[]::text[],'QANDEEL_HIM_CALCULATION_RUNTIME_V1','UNASSESSED',NULL,$4,'VERIFIER_ONLY') RETURNING result_state,numeric_value`,[randomUUID(),user,decision,randomUUID().toString()])).rows[0];
    assert.equal(zeroResult.result_state,'ASSESSED'); assert.equal(zeroResult.numeric_value,0);
    await client.query('SAVEPOINT dependency_failure'); await client.query(`INSERT INTO public.him_metric_definitions(metric_key,definition_version,canonical_name,canonical_definition,canonical_source,hif_owner,semantic_mapping_status,semantic_type,calculation_status,scale_reference,valid_context_kinds,required_input_contract,confidence_requirement_reference,dependency_ids) VALUES('test.unresolved',1,'Test only','Verifier fixture','verifier','HSE','RESOLVED','STATE','UNCALIBRATED','fixture-scale',ARRAY['DECISION'],'fixture-input','future-confidence-reference',ARRAY['missing.metric'])`); await assert.rejects(client.query('SET CONSTRAINTS him_metric_dependencies_valid IMMEDIATE')); await client.query('ROLLBACK TO SAVEPOINT dependency_failure'); await client.query('RELEASE SAVEPOINT dependency_failure');
    const call='SELECT * FROM public.create_him_metric_snapshot($1::jsonb)'; const observation={id:randomUUID(),metricKey:'test.synthetic.zero',definitionVersion:1,valueState:'UNASSESSED',supportingEvidenceIds:[`memory:${memory}`],contradictingEvidenceIds:[],contextKind:'DECISION',contextId:decision,scope:'exact decision',validityStatus:'VALID',descriptiveUpdateReason:'caller-authored verification description',descriptiveUpdateReferenceIds:[]};
    await identity(user); const first=(await client.query(call,[observation])).rows[0]; assert.equal(first.user_id,user); assert.equal(first.numeric_value,null); assert.equal(first.confidence_state,'UNASSESSED'); assert.equal(first.confidence_reference,null); assert.equal(first.snapshot_version,1); assert.equal(first.definition_version,1); assert.equal(first.context_id,decision); assert.deepEqual(first.source_engines,['QANDEEL_HIM_RUNTIME']); assert.equal(first.canonical_provenance,'QANDEEL_HIM_RUNTIME_FOUNDATION_V1'); assert.equal(first.descriptive_update_reason,observation.descriptiveUpdateReason);
    const second=(await client.query(call,[{...observation,id:randomUUID()}])).rows[0]; assert.equal(second.numeric_value,null); assert.equal(second.snapshot_version,2);
    const separate=(await client.query(call,[{...observation,id:randomUUID(),contextId:otherDecision}])).rows[0]; assert.equal(separate.snapshot_version,1);
    await rejects(call,[{...observation,id:randomUUID(),numericValue:0}]); await rejects(call,[{...observation,id:randomUUID(),valueState:'ASSESSED',numericValue:0}]); await rejects(call,[{...observation,id:randomUUID(),definitionVersion:2}]); await rejects(call,[{...observation,id:randomUUID(),supportingEvidenceIds:[`memory:${otherMemory}`]}]);
    for(const contextKind of ['RELATIONSHIP','DECISION','GOAL','CONVERSATION_SESSION']) await rejects(call,[{...observation,id:randomUUID(),contextKind,contextId:'caller-forged-context'}]);
    await rejects(call,[{...observation,id:randomUUID(),sourceEngines:['FORGED_CANONICAL_SOURCE']}]); await rejects(call,[{...observation,id:randomUUID(),canonicalProvenance:'FORGED_CANONICAL_PROVENANCE'}]); await rejects(call,[{...observation,id:randomUUID(),user_id:other,snapshot_version:99,observed_at:'2000-01-01'}]);
    await rejects("INSERT INTO public.him_metric_snapshots(id,user_id,metric_key,definition_version,semantic_mapping_status,semantic_type,value_state,source_engines,context_kind,context_id,scope,validity_status,snapshot_version,descriptive_update_reason,canonical_provenance) VALUES($1,$2,'test.synthetic.zero',1,'RESOLVED','TRAIT','UNASSESSED',ARRAY['FORGED'],'GLOBAL','GLOBAL','forged','VALID',99,'forged','FORGED')",[randomUUID(),user]);
    await identity(other); assert.equal((await client.query('SELECT * FROM public.him_metric_snapshots WHERE user_id=$1',[user])).rowCount,0); assert.equal((await client.query("SELECT * FROM public.him_metric_snapshots WHERE context_kind='GLOBAL'")).rowCount,0);
  } finally{ await client.query('ROLLBACK'); }
} finally{ await client.end(); }}
main().then(()=>console.log('Verified migration 0009 missingness/zero distinction, DB-enforced exact context identities, server-derived canonical source/provenance, descriptive update metadata, deterministic history, canonical definition identity, owned references, direct-RPC forgery resistance, and RLS isolation.')).catch(error=>{console.error(`HIM database verification failed (${error?.code??'verification'}): ${error?.message??'unknown assertion'}. Connection details were suppressed.`);process.exitCode=1;});
